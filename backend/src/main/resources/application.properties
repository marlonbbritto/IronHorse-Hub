spring.application.name=hub-api

# Datasource configuration (Default PostgreSQL)
spring.datasource.url=jdbc:postgresql://${DB_HOST:localhost}:5432/ironhorse
spring.datasource.username=${DB_USER:postgres}
spring.datasource.password=${DB_PASSWORD:postgres}
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect

# Server configuration
server.port=8080

# JWT Configuration
ironhorse.jwt.secret=NPvMQ5ktsYrqmOlO2Bm+P+gypq8UWoxKSxU13+iSv7M=
ironhorse.jwt.expiration=86400000
